//! Set up ISR table

const std = @import("std");
const hal = @import("hal");

// Note to future self, for syscalls use
//     asm volatile ("swi 0x0");
//     SVC handler

const ISR = *const fn () callconv(.C) void;

fn defaultHandler() callconv(.C) void {
    while (true) {}
}

pub const VectorTable = extern struct {
    stack_pointer: *anyopaque,
    Reset: ISR,
    NMI: ISR = defaultHandler,
    HardFault: ISR = defaultHandler,
    MemManage: ISR = defaultHandler,
    BusFault: ISR = defaultHandler,
    UsageFault: ISR = defaultHandler,
    _RESERVED0: u32 = 0,
    _RESERVED1: u32 = 0,
    _RESERVED2: u32 = 0,
    _RESERVED3: u32 = 0,
    SVC: ISR = defaultHandler,
    DebugMon: ISR = defaultHandler,
    _RESERVED4: u32 = 0,
    PendSV: ISR = defaultHandler,
    SysTick: ISR = defaultHandler,
    PVD_PVM: ISR = defaultHandler,
    _RESERVED5: u32 = 0,
    DTS: ISR = defaultHandler,
    IWDG: ISR = defaultHandler,
    WWDG: ISR = defaultHandler,
    RCC: ISR = defaultHandler,
    _RESERVED6: u32 = 0,
    _RESERVED7: u32 = 0,
    FLASH: ISR = defaultHandler,
    RAMECC: ISR = defaultHandler,
    FPU: ISR = defaultHandler,
    _RESERVED8: u32 = 0,
    _RESERVED9: u32 = 0,
    TAMP: ISR = defaultHandler,
    _RESERVED10: u32 = 0,
    _RESERVED11: u32 = 0,
    EXTI0: ISR = defaultHandler,
    EXTI1: ISR = defaultHandler,
    EXTI2: ISR = defaultHandler,
    EXTI3: ISR = defaultHandler,
    EXTI4: ISR = defaultHandler,
    EXTI5: ISR = defaultHandler,
    EXTI6: ISR = defaultHandler,
    EXTI7: ISR = defaultHandler,
    EXTI8: ISR = defaultHandler,
    EXTI9: ISR = defaultHandler,
    EXTI10: ISR = defaultHandler,
    EXTI11: ISR = defaultHandler,
    EXTI12: ISR = defaultHandler,
    EXTI13: ISR = defaultHandler,
    EXTI14: ISR = defaultHandler,
    EXTI15: ISR = defaultHandler,
    RTC: ISR = defaultHandler,
    SAES: ISR = defaultHandler,
    CRYP: ISR = defaultHandler,
    PKA: ISR = defaultHandler,
    HASH: ISR = defaultHandler,
    RNG: ISR = defaultHandler,
    ADC1_2: ISR = defaultHandler,
    GPDMA1_Channel0: ISR = defaultHandler,
    GPDMA1_Channel1: ISR = defaultHandler,
    GPDMA1_Channel2: ISR = defaultHandler,
    GPDMA1_Channel3: ISR = defaultHandler,
    GPDMA1_Channel4: ISR = defaultHandler,
    GPDMA1_Channel5: ISR = defaultHandler,
    GPDMA1_Channel6: ISR = defaultHandler,
    GPDMA1_Channel7: ISR = defaultHandler,
    TIM1_BRK: ISR = defaultHandler,
    TIM1_UP: ISR = defaultHandler,
    TIM1_TRG_COM: ISR = defaultHandler,
    TIM1_CC: ISR = defaultHandler,
    TIM2: ISR = defaultHandler,
    TIM3: ISR = defaultHandler,
    TIM4: ISR = defaultHandler,
    TIM5: ISR = defaultHandler,
    TIM6: ISR = defaultHandler,
    TIM7: ISR = defaultHandler,
    TIM9: ISR = defaultHandler,
    SPI1: ISR = defaultHandler,
    SPI2: ISR = defaultHandler,
    SPI3: ISR = defaultHandler,
    SPI4: ISR = defaultHandler,
    SPI5: ISR = defaultHandler,
    SPI6: ISR = defaultHandler,
    HPDMA1_Channel0: ISR = defaultHandler,
    HPDMA1_Channel1: ISR = defaultHandler,
    HPDMA1_Channel2: ISR = defaultHandler,
    HPDMA1_Channel3: ISR = defaultHandler,
    HPDMA1_Channel4: ISR = defaultHandler,
    HPDMA1_Channel5: ISR = defaultHandler,
    HPDMA1_Channel6: ISR = defaultHandler,
    HPDMA1_Channel7: ISR = defaultHandler,
    SAI1_A: ISR = defaultHandler,
    SAI1_B: ISR = defaultHandler,
    SAI2_A: ISR = defaultHandler,
    SAI2_B: ISR = defaultHandler,
    I2C1_EV: ISR = defaultHandler,
    I2C1_ER: ISR = defaultHandler,
    I2C2_EV: ISR = defaultHandler,
    I2C2_ER: ISR = defaultHandler,
    I2C3_EV: ISR = defaultHandler,
    I2C3_ER: ISR = defaultHandler,
    USART1: ISR = defaultHandler,
    USART2: ISR = defaultHandler,
    USART3: ISR = defaultHandler,
    UART4: ISR = defaultHandler,
    UART5: ISR = defaultHandler,
    UART7: ISR = defaultHandler,
    UART8: ISR = defaultHandler,
    I3C1_EV: ISR = defaultHandler,
    I3C1_ER: ISR = defaultHandler,
    OTG_HS: ISR = defaultHandler,
    ETH: ISR = defaultHandler,
    CORDIC: ISR = defaultHandler,
    GFXTIM: ISR = defaultHandler,
    DCMIPP: ISR = defaultHandler,
    LTDC: ISR = defaultHandler,
    LTDC_ER: ISR = defaultHandler,
    DMA2D: ISR = defaultHandler,
    JPEG: ISR = defaultHandler,
    GFXMMU: ISR = defaultHandler,
    I3C1_WKUP: ISR = defaultHandler,
    MCE1: ISR = defaultHandler,
    MCE2: ISR = defaultHandler,
    MCE3: ISR = defaultHandler,
    XSPI1: ISR = defaultHandler,
    XSPI2: ISR = defaultHandler,
    FMC: ISR = defaultHandler,
    SDMMC1: ISR = defaultHandler,
    SDMMC2: ISR = defaultHandler,
    _RESERVED12: u32 = 0,
    _RESERVED13: u32 = 0,
    OTG_FS: ISR = defaultHandler,
    TIM12: ISR = defaultHandler,
    TIM13: ISR = defaultHandler,
    TIM14: ISR = defaultHandler,
    TIM15: ISR = defaultHandler,
    TIM16: ISR = defaultHandler,
    TIM17: ISR = defaultHandler,
    LPTIM1: ISR = defaultHandler,
    LPTIM2: ISR = defaultHandler,
    LPTIM3: ISR = defaultHandler,
    LPTIM4: ISR = defaultHandler,
    LPTIM5: ISR = defaultHandler,
    SPDIF_RX: ISR = defaultHandler,
    MDIOS: ISR = defaultHandler,
    ADF1_FLT0: ISR = defaultHandler,
    CRS: ISR = defaultHandler,
    UCPD1: ISR = defaultHandler,
    CEC: ISR = defaultHandler,
    PSSI: ISR = defaultHandler,
    LPUART1: ISR = defaultHandler,
    WAKEUP_PIN: ISR = defaultHandler,
    GPDMA1_Channel8: ISR = defaultHandler,
    GPDMA1_Channel9: ISR = defaultHandler,
    GPDMA1_Channel10: ISR = defaultHandler,
    GPDMA1_Channel11: ISR = defaultHandler,
    GPDMA1_Channel12: ISR = defaultHandler,
    GPDMA1_Channel13: ISR = defaultHandler,
    GPDMA1_Channel14: ISR = defaultHandler,
    GPDMA1_Channel15: ISR = defaultHandler,
    HPDMA1_Channel8: ISR = defaultHandler,
    HPDMA1_Channel9: ISR = defaultHandler,
    HPDMA1_Channel10: ISR = defaultHandler,
    HPDMA1_Channel11: ISR = defaultHandler,
    HPDMA1_Channel12: ISR = defaultHandler,
    HPDMA1_Channel13: ISR = defaultHandler,
    HPDMA1_Channel14: ISR = defaultHandler,
    HPDMA1_Channel15: ISR = defaultHandler,
    GPU2D: ISR = defaultHandler,
    GPU2D_ER: ISR = defaultHandler,
    ICACHE: ISR = defaultHandler,
    FDCAN1_IT0: ISR = defaultHandler,
    FDCAN1_IT1: ISR = defaultHandler,
    FDCAN2_IT0: ISR = defaultHandler,
    FDCAN2_IT1: ISR = defaultHandler,
};
